{
  "name": "@medinext/organ-donor-service",
  "version": "1.0.0",
  "description": "MediNext Organ Donor Service - Organ donor registry and matching",
  "main": "dist/main.js",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main",
    "test": "jest --config ../../packages/backend/shared/jest.config.js"
  },
  "dependencies": {
    "@medinext/shared": "*"
  }
}
